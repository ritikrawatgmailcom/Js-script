const a=400
console.log(a);

const b= new Number(200)  // it help to define type also ki type kya h 
console.log(b);

console.log(b.toString());
console.log(b.toFixed(2)); //it will help to add points after number

const c=123.899
console.log(c.toPrecision(4));
