const name="Ritik"
const count=4
console.log(`My name is ${name} and count is ${count}`);

// we can also define our string
const gamename=new String ("Ritik") // by using it we can access or use many methods in string.
console.log(gamename[0]);
console.log(gamename.split());

